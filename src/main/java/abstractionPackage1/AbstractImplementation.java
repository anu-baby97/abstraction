package abstractionPackage1;

public class AbstractImplementation extends AbstractClassExample { 
	/*If a abstract class is inherited to non abstract class, 
	then the abstract methods declared in the abstract class must be defined in the non-abstract or normal class*/

	public void process() {
		
		System.out.println("This is abstract method being defined");
	}
	
	//public abstract details();
	
}
